# CoinMarketCap Pro API key (https://coinmarketcap.com/api/).
# Copy this file to `.env` and set your key. Do not commit `.env`.
# This variable is read only by the Vite dev/preview server for the `/cmc-api`
# proxy; it is not exposed in the client JavaScript bundle.

CMC_API_KEY=
